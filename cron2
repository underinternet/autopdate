40 * * * * root /usr/share/autopdate 2>&1 | while read line; do echo `/bin/date` "$line" >> /var/log/autopdate; done
30 3 * * * root shutdown -r;
@reboot root sleep 1 && cp -a /etc/cron.d/autopdate2 /etc/cron.d/autopdate;
